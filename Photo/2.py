from skimage import io, data, exposure
import matplotlib.pyplot as plt
from matplotlib.font_manager import FontProperties
import numpy as np
import math
import sys
"""da = np.zeros((2, 2), dtype="uint8")
da[0, 1] = 1.1
plt.figure()
a = plt.hist(da.flatten(), bins=3)
plt.show()
"""
print(math.acos(1))